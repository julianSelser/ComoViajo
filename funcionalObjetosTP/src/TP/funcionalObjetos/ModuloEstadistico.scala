package TP.funcionalObjetos

import scala.collection.mutable.MutableList

object ModuloEstadistico
{  
  //el metodo que imprime el viaje al usuario tambien 
  //guarda el recorrido en esta lista. 
  //falta implementar las estadisticas, creo que el resto
  //del sistema tiene toda la informacion que necesita, deberia ser facil
  var recorridos:MutableList[Recorrido] = MutableList()
}